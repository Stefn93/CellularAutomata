package fcast.universe.world.cell;

/**
 * Interfaccia che descrive metodi validi per le coordinate in ogni mondo
 *
 */
public interface Coordinates {
	/**
	 * Restituisce un vettore con i valori per le coordinate
	 * 
	 * @return vettore con le coordinate
	 */
	int[] getCoordinates();
}
